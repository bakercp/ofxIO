language: c++
compiler: gcc
# sudo: true
# #matrix:
# #  include:
#   - os: linux
#     dist: trusty
#     sudo: required
#     env: TARGET="linux"
#     addons:
#       apt:
#         sources:
#           - ubuntu-toolchain-r-test
#         packages:
#           - gcc-4.9
#           - g++-4.9
#           - gdb
#
#   # - os: osx
#   #   osx_image: xcode8
#   #   compiler: clang
#   #   env:
#   #     - TARGET="osx"
#   exclude:
#   - compiler: gcc
#
# env:
#   global:
#     - CI_REPO=https://github.com/bakercp/ofxAddonsCI.git
#
# git:
#   depth: 1

# All files in the build-lifecycle
# https://docs.travis-ci.com/user/customizing-the-build/#The-Build-Lifecycle
before_install:
  - git clone $CI_REPO --depth=1 --branch="$TRAVIS_BRANCH" ~/scripts 2> /dev/null
  - ~/scripts/before_install.sh
install:
  - ~/scripts/install.sh
before_script:
  - ~/scripts/before_script.sh
script:
  - ~/scripts/script.sh
after_success:
  - ~/scripts/after_success.sh
after_failure:
  - ~/scripts/after_failure.sh
before_deploy:
  - ~/scripts/before_deploy.sh
deploy:
  - ~/scripts/deploy.sh
after_deploy:
  - ~/scripts/after_deploy.sh
after_script:
  - ~/scripts/after_script.sh
